{
  "id": "316467cb",
  "title": "Wire up index.ts + integration",
  "tags": [
    "debugger",
    "integration"
  ],
  "status": "done",
  "created_at": "2026-06-24T00:53:43.432Z"
}

Done. `index.ts` composes all parts; integration verified end-to-end (real pi load + 14-assertion mock-API integration test covering: factory registration, resources_discover skill contribution, session_start widget install, /debug local start, live packet ingest, hypothesis→fix→"Bug Fixed"→cleanup→summary→exit, /debug stop idempotency, before_agent_start gating, session_shutdown server close).

Server started lazily from /debug (not the factory), as required. Restore/teardown on session_start/session_shutdown. No package.json needed (node built-ins + pi packages only). README documents install locations (~/.pi/agent/extensions/debugger global, or .pi/extensions/debugger project-local) and `PI_DEBUG_PORT` config.

Open follow-ups (non-blocking): (1) wireframe-driven widget polish; (2) ngrok remote path needs a real run with ngrok installed.
