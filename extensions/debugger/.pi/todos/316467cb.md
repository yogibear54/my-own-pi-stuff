{
  "id": "316467cb",
  "title": "Wire up index.ts + integration",
  "tags": [
    "debugger",
    "integration"
  ],
  "status": "open",
  "created_at": "2026-06-24T00:53:43.432Z"
}

Compose the parts into the extension entry. **Reference: [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).**

Scope:
- `index.ts` default export factory: register commands (Part 2), tools (Parts 4 & 5), and event handlers.
- Wire Part 1 server's `onPacket` → Part 3 widget buffer.
- Bind Part 5 state to Part 3 widget rendering and Part 2 command transitions.
- Restore/teardown on `session_start` / `session_shutdown`.
- Confirm install location (`~/.pi/agent/extensions/debugger` global, or `.pi/extensions/debugger` project-local) and that no `package.json` is needed (node built-ins only).

Do not start the server from the factory (defer to `/debug`).

Integration checklist:
- [ ] `/debug` → server up, widget shows, state = AWAITING CONTEXT
- [ ] injected snippet → packet → 200 → appears in widget log stream + JSONL file
- [ ] user "Bug Fixed" → telemetry removed, fix kept, summary shown
- [ ] `/debug stop` → full teardown, log file retained
- [ ] `/resume` reconstructs state + snippet map for cleanup
