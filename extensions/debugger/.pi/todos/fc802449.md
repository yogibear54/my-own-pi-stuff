{
  "id": "fc802449",
  "title": "Snippet injection & cleanup tools (Part 4)",
  "tags": [
    "debugger",
    "part-4",
    "tools"
  ],
  "status": "done",
  "created_at": "2026-06-24T00:53:43.432Z"
}

Done. `snippets.ts` (pure) + tools wired in `index.ts`.

Verified (11 pure assertions + integration):
- Block style `/* AI_DEBUG_SNIPPET_START:ID=n NAME="..." */ ... /* END */` and line style `#` (Python/Ruby/Shell).
- `commentStyleFor` maps extensions → block/line.
- ROUND-TRIP byte-clean for both styles (inject→removeById restores original exactly).
- `removeAllSnippets` keeps non-snippet edits (a "fix" survives cleanup).
- Tools `inject_snippet` / `remove_snippet` / `list_snippets` / `cleanup_all_snippets` run via `withFileMutationQueue`, normalize leading `@`, track `{id→{file,name,line}}` in state.
- Integration test: real tool writes delimited block to a temp file, tracks it, and `mark_bug_fixed`/`cleanup_all_snippets` restores the file byte-for-byte.

Prompt guidance (promptSnippet/promptGuidelines) registered so the model uses delimiters + correct target. Parser pairs START→next-END (snippets are sequential, never nested).
